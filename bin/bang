#!/usr/bin/env node

var path  = require('path'),
    fs    = require('fs'),
    Bang  = require(path.join(path.dirname(fs.realpathSync(__filename)), '../lib/bang')),
    prog  = new Bang;

prog.start();
